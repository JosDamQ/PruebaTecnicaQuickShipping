<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CRUD</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
</head>
<body>
    
    <nav class="navbar navbar-dark bg-dark">
        <a href="/moneda/getAll" class="navbar-brand">Cambios</a>
    </nav>

    <div class="container">
        <div class="row mt-5">
            <div class="col md-7">
                <table class="table table-bordered table-hover">
                    <thead>
                        <tr>
                            <th>Id</th>
                            <th>Nombre de moneda</th>
                            <th>venta</th>
                            <th>compra</th>
                            <th>acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% if(data)  { %>
                            <% for (var i = 0; i < data.length; i++){ %>
                                <tr>
                                    <td><%= (i + 1) %></td>
                                    <td><%= data[i].nombre %></td>
                                    <td><%= data[i].venta %></td>
                                    <td><%= data[i].compra %></td>
                                    <td>
                                        <a href="/moneda/delete/<%= data[i].idMoneda%>" class="btn btn-danger">Eliminar</a>
                                        <a href="/moneda/getOne/<%= data[i].idMoneda%>" class="btn btn-warning">Actualizar</a>
                                    </td>
                                </tr>
                            <%}%>

                        <%}%>
                    </tbody>
                </table>
            </div>
            <div class="col-md-5">
                <div class="card">
                    <div class="card-body">
                        <form action="/moneda/save" method="post">
                            <div class="form-group">
                                <input type="text" name="nombre" value="" placeholder="nombre de moneda" class="form-control">
                            </div>
                            <div class="form-group">
                                <input type="number" step="0.01" name="venta" value="" placeholder="valor venta" class="form-control">
                            </div>
                            <div class="form-group">
                                <input type="number" step="0.01" name="compra" value="" placeholder="valor de compra    " class="form-control">
                            </div>
                            <button type="submit" class="btn btn-success">Agregar</button>
                        </form>
                    </div>
                    <a href="/moneda/api" class="btn btn-primary">API</a>
                </div>
            </div>
        </div>
    </div>

</body>
</html>